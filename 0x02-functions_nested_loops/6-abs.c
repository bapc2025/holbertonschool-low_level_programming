#include "main.h"

/**
 * _abs - computes absolute value of and integer.
 * @n: integer to be computed.
 *
 * return: absolute value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
